machine:
  node:
    version: 6.10.2
  environment:
    NODE_MODE: development
dependencies:
  override:
    - ./build.sh
  cache_directories:
    - ~/.cache/yarn
deployment:
  production:
    commands:
      - scp edublocks.tar.xz Josh@edublocks.org:/var/www/html/
